package com.project.recon.config;

import org.springframework.context.annotation.Configuration;

/**
 * Spring Batch configuration for Boot 3.
 * We rely on default in-memory JobRepository for demo simplicity.
 */
@Configuration
public class BatchConfig {
    // Intentionally empty for Spring Batch 5+
}
